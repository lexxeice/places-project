<%= form_with model: @place, id: 'placeUpdate' do |f| %>
    <div class="input-group">
      <div>
        <%= f.text_field :title, value: '',
                                 id: 'title-conf',
                                 name: 'title',
                                 maxlength: 50,
                                 required: true,
                                 autofocus: true,
                                 placeholder: t('.title'),
                                 class: 'form-control m-1 p-1 aa_input' %>
        <%= f.text_area :description, value: '',
                                      id: 'description-conf',
                                      maxlength: 300,
                                      required: true,
                                      placeholder: t('.description'),
                                      class: 'form-control m-1 p-1 aa_input' %>
      </div>
      <div class="input-group-append">
        <%= f.submit t('.update_button'), class: 'btn btn-outline-success m-1 p-1', id: 'btn-upd-conf' %>
      </div>
    </div>
    <%= hidden_field_tag(:coordinates) %>
<% end %>
